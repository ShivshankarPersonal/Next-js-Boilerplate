import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Form = () => {
  return (
    <Main meta={<Meta title="Forms - Enter Daily Details" description="Data to Store in Database" />}>
      <p>
        React-Form-Hooks
      </p>
    </Main>
  );
}

export default Form;


